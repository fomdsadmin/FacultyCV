import React from "react";

const AttributeGroup = ({ attributeGroup, setAttributeGroup }) => {
  return (
    <div style={{ fontSize: 12, color: "#666" }}>
      Attribute Group settings panel
    </div>
  );
};

export default AttributeGroup;
