import React from "react";

const Attribute = ({ attributeGroup, setAttributeGroup }) => {
  return (
    <div style={{ fontSize: 12, color: "#666" }}>
      Attribute settings panel
    </div>
  );
};

export default Attribute;
